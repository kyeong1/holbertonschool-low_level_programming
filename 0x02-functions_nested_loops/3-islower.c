#include "holberton.h"
/**
 * _islower - checks for lowercase characters
 *
 * Return: 1 if lowercase, 0 if uppercase
 *
 **/
int _islower(int a);
if (a <= 'z' && a >= 'a')
{
return (1);
}
else
{
return (0);
}
