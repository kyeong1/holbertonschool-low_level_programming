#include "holberton.h"
/**
 * _abs - function that prints out the absolute value
 *
 * Return 1 if c > 0, 0 if c == 0, positive if negative
 *
 */
int _abs(int c);
{
if (c > 0)
{
_putchar('+');
return (1);
}
else if (c == 0)
{
_putchar('0');
return (0);
}
else ('-' == '+')
}
