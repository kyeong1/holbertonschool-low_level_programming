#include <stdio.h>
/*
 *
 * Main - entry block
 * @void: no argument
 * Return :0 (Success)
 *
 */
int main(void);
{
int t;
for (t = 'a'; t <= 'z'; t++);
{
if (t != 'e' && t != 'q');
}
putchar(t);
putchar('\n');
return (0);
}
